@typeparam TItem

 @if (this.RenderItems?.Count() > 0)
{
    @foreach (var item in this.RenderItems)
    {
        <div class="waterfall-item" style="@(item.Style)">@(this.VirtualList?.ItemTemplate(item.Data))</div>
    }
}